{
  "$id": "CurrentState",
  "type": "object",
  "properties": {
    "meetingSummary": { "type": "string" },
    "workflows": {
      "type": "array",
      "items": { "$ref": "workflow.schema.json" }
    }
  },
  "required": ["meetingSummary", "workflows"]
}

